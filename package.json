{
  "name": "article-summarizer-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that summarizes the current article with Claude. Personal use, own API key.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "icons": "node scripts/make-icons.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
